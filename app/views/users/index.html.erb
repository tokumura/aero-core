<h1>Listing users</h1>

<table border="1">
  <tr>
    <th>Name</th>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
<%# if user.try(:admin?)  %>
<% if user.admin  %>
    <td>Admin</td>
<% else %>
    <td>not Admin</td>
<% end %>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
    <td>
      <%= link_to 'Grant Admin', make_admin_user_path(user) %>
    </td>
    <td>
      <%= link_to 'Remove Admin', remove_admin_user_path(user) %>
    </td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Top', products_path %><br/>
<%= link_to 'New User', new_user_path %><br/>
<%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
